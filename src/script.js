const { title } = require("process");

let titleElement = document.getElementById('title');
console.log(titleElement, titleElement instanceof HTMLElement);
console.log(titleElement instanceof HTMLElement);
